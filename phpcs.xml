<?xml version="1.0"?>
<ruleset name="WP2 Coding Standards">
    <description>WP2: Lint only our source code using WordPress standard.</description>

    <!-- Use WordPress Core standard -->
    <rule ref="WordPress" />

    <!-- Limit scanning to our PHP source directory -->
    <file>wp-content/mu-plugins/wp2-download/src</file>

    <!-- Extensions and performance -->
    <config name="extensions" value="php"/>
    <arg name="parallel" value="4"/>
    <arg value="ps"/>

    <!-- Exclusions -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>wp-content/uploads/*</exclude-pattern>
    <exclude-pattern>wp-content/cache/*</exclude-pattern>
    <exclude-pattern>*.min.php</exclude-pattern>
</ruleset>